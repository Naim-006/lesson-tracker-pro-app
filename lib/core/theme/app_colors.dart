import 'package:flutter/material.dart';

/// Lesson Tracker Pro — Sunset palette (logo + UI mockups).
class AppColors {
  static const navy = Color(0xFF1B263B);
  static const navyDark = Color(0xFF0B121E);
  static const sunset = Color(0xFFE07A5F);
  static const sunsetBright = Color(0xFFF97316);
  static const primary = sunset;
  static const cream = Color(0xFFFDFBF0);
  static const creamAlt = Color(0xFFFFF9F5);

  // Light
  static const lightBg = creamAlt;
  static const lightCard = Colors.white;
  static const lightText = Color(0xFF1A1C22);
  static const lightMuted = Color(0xFF8E8E93);
  static const lightBorder = Color(0xFFE8E4DE);

  // Dark
  static const darkBg = Color(0xFF0F172A);
  static const darkCard = Color(0xFF1E293B);
  static const darkCardElevated = Color(0xFF243447);
  static const darkText = Colors.white;
  static const darkMuted = Color(0xFF94A3B8);
  static const darkBorder = Color(0xFF334155);

  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFFBBF24);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);
}
